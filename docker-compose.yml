services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: inventory-dashboard
    restart: unless-stopped
    networks:
      - internal-net

networks:
  internal-net:
    external: true
